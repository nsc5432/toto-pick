# 통계 기반 수익 모델 설계서

목적: `research` 탐색 파이프라인에 태울 **통계학적 근거가 있는 알고리즘 후보**를 정의한다.
이 문서는 설계서다 — 구현은 후보별로 별도 요청 시 진행하고, 구현 전에는 반드시
[MCP SQL 사전 검증](#3-sql-사전-검증--구현-전-필수-관문)을 통과해야 한다.

목표(`application.yaml > research.goal`, 요청별 변경 불가):

| 기준 | 값 | 의미 |
|---|---|---|
| 검증 수익률 | ≥ +10% | (output−input)/input, **검증 구간만** |
| 최소 조합 수 | ≥ 100 | 미만이면 운 — 실격 |
| 최소 베팅일 | ≥ 20 | 특정 기간 몰빵 방지 |

---

## 1. 현황 진단 — 원장이 말해주는 것

`research/experiments.jsonl` 14건 전원 미달성. 대표 실패 패턴(MARKET_EDGE):

- `combinedN=4~5` 스윕 → 학습 적중률 ~1%, 검증 적중 **0건**, 검증 수익률 −100%
- 학습에서 +4~6%가 나온 후보도 검증에서 전멸 → 전형적인 **소표본 행운 + 과최적화**

### 왜 combinedN이 크면 구조적으로 지는가 (파룰레이 수학)

경기 하나의 배당에는 하우스 마진 m이 섞여 있다 (오버라운드 = 역배당 합 − 1, 통상 15~25%).
N경기 조합의 기대값은 다리마다 마진이 **곱으로 누적**된다:

```
EV(조합) ≈ (1 − m_유효)^N − 1        (모델 엣지가 0일 때)
m_유효 = 0.05 가정 시:  N=2 → −9.8%,  N=3 → −14.3%,  N=5 → −22.6%
```

즉 목표 +10%를 넘으려면 다리당 요구되는 엣지가 N에 따라 커진다. 동시에 적중 확률은
p^N으로 붕괴해 분산이 폭발하고, 100조합 표본으로는 참 수익률의 신뢰구간이 목표를 덮지 못한다.

> **설계 결정 D1**: 이후 모든 후보의 `combinedN` 탐색 범위는 **2~3**을 기본으로 한다.
> 4 이상은 다리당 엣지가 SQL로 입증된 경우에만 허용.

또 하나의 교훈: 현재 확률 추정이 "최근 N경기 승률 + 상수 스무딩"뿐이다. 승/패만 세는 승률은
정보량이 낮고 노이즈가 크다. **개선 지렛대는 (a) 확률 추정의 품질, (b) 조합 구성** 두 가지다.

---

## 2. 모델 후보 (우선순위 순)

각 후보: 가설 → 통계 근거 → 스키마 충족 여부 → 노브(ParamSpec) → 검증 방법.
계열 분류는 `.claude/skills/algo-search/references/taxonomy.md` 기준.

### M1. 피타고리안 기대승률 × 시장 괴리 — 계열 C 변형 ★ 최우선

- **가설**: 득점/실점은 승패보다 정보량이 많다. 점수 기반 기대승률이 시장 확률과 괴리된 쪽이 +EV.
- **통계 근거**: Bill James 피타고리안 기대승률 — 야구에서 실제 승률보다 미래 승률을 더 잘 예측함이
  반복 검증된 고전적 결과. 승패는 1비트, 점수차는 연속 정보.

  ```
  W% = RS^k / (RS^k + RA^k)        RS=득점합, RA=실점합, k ≈ 1.8 (MLB 기준, KBO는 스윕으로 결정)
  ```
- **데이터**: `res1`/`res2`(스코어)가 `baseball_result`에 이미 있음 → **스키마 충족**.
- **구현 방향**: `MARKET_EDGE`의 폼 승률 부분만 피타고리안 추정으로 교체. de-vig 로직은 그대로 재사용.
  `TeamFormIndex`에 득실 집계를 추가하거나 별도 인덱스로 — 단, **날짜 인자를 받는 lookahead-safe
  구조는 동일하게 유지**.
- **노브**: `num`(집계 경기 수 15~45), `pythExponent`(1.5~2.2), `edgeThreshold`(0.03~0.15),
  `minOdds`(1.3~2.0), `combinedN`(2~3)
- **사전 검증 SQL**: 최근 N경기 득실비 상위 팀 vs 하위 팀의 이후 경기 실제 승률 차이가
  시장 내재 확률 차이보다 큰지 확인.

### M2. 수축 추정(Empirical Bayes) 폼 — 계열 B/C 공통 기반

- **가설**: 원시 `winPercent`는 표본 10~20경기짜리 노이즈다. 리그 평균으로 수축(shrink)하면
  추정이 안정되어 같은 알고리즘도 성능이 오른다.
- **통계 근거**: Beta-Binomial 수축 — 소표본 비율 추정의 표준 처방. 현재 `FORM_SMOOTHING=5.0`
  상수가 이것의 조악한 형태인데, 수축 강도가 근거 없이 고정돼 있고 스윕도 안 된다
  (→ CLAUDE.md의 "상수는 검증되지 않는다" 규칙 위반 상태).

  ```
  p̂ = (승수 + α) / (경기수 + α + β)      α/(α+β) = 리그 평균 승률, α+β = 수축 강도(노브)
  ```
- **데이터**: 추가 데이터 불필요 → **스키마 충족**.
- **구현 방향**: 단독 알고리즘이 아니라 **M1/기존 MARKET_EDGE의 확률 추정 개선**으로 적용.
  `shrinkStrength`를 ParamSpec으로 승격해 스윕이 최적 수축 강도를 고르게 한다.
- **노브**: `shrinkStrength`(2~30)

### M3. 배당 구조 신호 — 계열 E (미개척)

- **가설**: 배당의 **모양** 자체가 정보다. 특히 ① 오버라운드가 작은 경기 = 북메이커가 확신하는
  경기 → 페이버릿 추종 성공률↑, ② favorite–longshot bias — 저배당이 내재 확률 대비 저평가되는
  경향은 베팅 시장 문헌에서 가장 재현성 높은 편향.
- **통계 근거**: 배당 구간별 (실제 적중률 − de-vig 내재 확률)을 비교하는 **캘리브레이션 분석**.
  양(+)인 구간이 있으면 그 구간만 베팅하는 것이 곧 전략이 된다.
- **데이터**: `homeDiv/drawDiv/awayDiv`만 사용, 폼 히스토리 불필요 → **스키마 충족**,
  시즌 초에도 작동한다는 부가 장점.
- **구현 방향**: 신규 `ODDS_STRUCTURE` 알고리즘. de-vig 후 내재 확률이 특정 구간에 들고
  오버라운드가 임계 이하인 경기의 페이버릿(또는 SQL이 지목한 구간)만 선택.
- **노브**: `probLow`/`probHigh`(선택 구간), `maxOverround`, `combinedN`(2~3)
- **사전 검증 SQL** (이 계열은 SQL 결과가 곧 전략 — 반드시 먼저 실행):

  ```sql
  -- 내재 확률 십분위별 캘리브레이션: actual_rate > implied 인 구간이 엣지
  SELECT FLOOR(implied_home * 10) AS bucket,
         COUNT(*) AS n,
         AVG(implied_home) AS implied,
         AVG(CASE WHEN res1 > res2 THEN 1 ELSE 0 END) AS actual_rate
  FROM (
    SELECT res1, res2,
           (1/homeDiv) / (1/homeDiv + 1/drawDiv + 1/awayDiv) AS implied_home
    FROM baseball_result
    WHERE homeDiv IS NOT NULL AND drawDiv IS NOT NULL AND awayDiv IS NOT NULL
  ) t
  GROUP BY bucket ORDER BY bucket;
  ```

### M4. Elo / Bradley–Terry 레이팅 — 계열 C 고급 변형

- **가설**: 경기 단위로 갱신되는 레이팅은 "최근 N경기 승률"보다 상대 전력·최신성을 정교하게 반영한다.
- **통계 근거**: Elo는 Bradley–Terry 모델의 온라인 근사. 홈 어드밴티지를 상수 H로 자연스럽게 흡수:

  ```
  P(홈승) = 1 / (1 + 10^(−(R_home + H − R_away)/400))
  갱신:  R ← R + K·(결과 − 기대)
  ```
- **데이터**: 승패(+점수차로 K 보정 가능) → **스키마 충족**.
- **구현 방향**: `TeamFormIndex`처럼 **런당 1회, 날짜순으로 구축**하는 레이팅 인덱스
  (날짜 인자로 조회해 lookahead 차단 — 기존 패턴 그대로). 확률을 M1과 같은 괴리 비교에 투입.
- **노브**: `kFactor`(8~40), `homeAdvantage`(0~100 Elo점), `edgeThreshold`, `combinedN`(2~3)
- **주의**: 구현 비용이 가장 크다. M1이 실패한 뒤에 착수.

### M5. 조합 구성 개선 — 계열 F (신호 계열과 반드시 짝지어 평가)

- **가설**: 같은 신호라도 묶는 법이 수익률을 바꾼다. 현재 "정렬 후 N개씩 자르고 나머지 버림"은
  약한 신호끼리 한 조합에 몰리는 구조.
- **후보**: ① `combinedN=2` 고정 + 강신호만, ② 조합 배당곱이 목표 구간(예 2.5~5.0)에 들도록 greedy
  구성, ③ 최강 신호 1개 + 저배당 안정 다리 1개 혼합.
- **평가 규칙**: 단독 계열이 아니므로 M1 또는 M3에 얹어 **동일 신호·다른 구성**으로 A/B 비교.

### M6. 일정·맥락 신호 — 계열 D (보류)

휴식일 간격·연속 원정은 `ymd`/`tm`에서 파생 가능하지만, 효과 크기가 작아 마진(15%+)을 단독으로
넘기 어렵다는 것이 야구 문헌의 일반적 결과. **M1~M3의 보조 필터**로만 검토. 구장·선발투수는
현재 스키마로 불가 — 설계 금지.

---

## 3. SQL 사전 검증 — 구현 전 필수 관문

`kbo-mysql` MCP(읽기 전용)로 다음을 순서대로 확인한 뒤에만 구현에 착수한다.
**SQL에서 안 보이는 신호는 스윕에서도 안 나온다** — 구현·스윕 비용이 훨씬 비싸다.

1. **마진 실측**: `AVG(1/homeDiv + 1/drawDiv + 1/awayDiv) − 1` — 넘어야 할 허들의 실제 크기.
2. **캘리브레이션 테이블** (M3 쿼리): 십분위별 `actual − implied`. 여기서 +2%p 이상 나는 구간이
   없으면 C/E 계열 전체의 기대치를 낮춰 잡아야 한다.
3. **후보별 신호 존재 확인**: 예) M1 — 최근 20경기 득실비 상위/하위 팀의 이후 승률 스프레드가
   시장 스프레드보다 큰가.
4. **표본 수 확인**: 필터 통과 경기 수 × 기간으로 조합 100개·베팅일 20일이 나오는지 역산.
   여기서 미달이면 필터를 완화하거나 후보를 버린다 (스윕 후 "표본 부족" 진단의 사전 차단).

주의: SQL 검증도 **학습 구간(YMD 기준 앞 70%)만** 대상으로 한다. 전체 기간으로 신호를 찾으면
그 자체가 검증 구간 누수다.

---

## 4. 통계적 검증 규율

파이프라인이 이미 강제하는 것(학습/검증 분리, 최소 표본 게이트, 시드 고정, 원장 기록) 위에
사람이 지켜야 할 것:

- **다중 비교 문제**: 후보 200개를 스윕하면 우연히 좋아 보이는 것이 반드시 나온다. 격자는
  노브당 3~5단계로 작게, 검증 재측정은 top-K(5)만 — 파이프라인 기본값을 넓히지 말 것.
- **검증 숫자로 파라미터를 고르지 않는다** — 재탐색 사유는 오직 학습 진단(표본 부족 등).
- **재시드 금지**: seed=42 고정. 시드를 바꿔 다시 돌리는 것은 p-hacking이다.
- **분산 감각**: 조합 100개·평균 배당 3.0이면 수익률 표준편차가 ±15%p를 넘는다.
  검증 +10%는 "달성"이지 "증명"이 아니다 — 원장에 축적해 재현성으로 판단한다.
- 보고는 항상 **검증 지표**로. 학습 지표 보고는 anti-pattern (`algo-search` SKILL 참조).

---

## 5. 구현 시 공통 규칙 (CLAUDE.md 요약 + 이 문서의 결정)

- `TunableAlgorithm` 구현 — **모든 임계값·상수는 `paramSpace()`에 선언** (M2의 수축 강도 포함).
- 팀 폼·레이팅은 `input.formIndex()` 패턴 — 날짜 인자를 받는 인덱스를 런당 1회 구축, `historyGames` 직접 순회 금지.
- 순수 로직이면 `pick/domain/` + `PickAlgorithmConfig` `@Bean`, 데이터 의존이면 `pick/infrastructure/algorithm/` `@Component`.
- `code()`는 영구 식별자 — 한 번 원장/DB에 남으면 개명 금지.
- `combinedN` 기본 탐색 범위 2~3 (설계 결정 D1).
- 승자 확정 후에만 `POST /api/picks/simulate`로 운영 테이블 기록.

## 6. 실행 로드맵

| 순서 | 작업 | 선행 조건 | 상태 |
|---|---|---|---|
| 1 | SQL 관문 1·2 (마진 실측 + 캘리브레이션) | 없음 — 즉시 가능 | ✅ 완료 (§7) |
| 2 | M3 배당 구조 (SQL 결과가 곧 전략, 구현 최소) | 관문 2에서 +구간 발견 | ❌ **부결** — §7·§9, 오버라운드 사실상 상수 |
| 3 | M1 피타고리안 괴리 + M2 수축 적용 | M1 신호 SQL 확인 | ❌ **부결** — 사전 검증 실패 (§8) |
| 4 | M5 조합 구성 A/B (M1 또는 M3 위에) | 2 또는 3에서 생존 후보 | ❌ 선행 생존 후보 없음 |
| 5 | M4 Elo (고비용 — 앞 단계 실패 시) | M1 실패 진단 | ⚠ 보류 — §8 결과상 기대치 낮음 |
| 6 | **원시 발표 배당 확보 → D2 해제** | — | ✅ 완료 (§9) — C 계열 재개 가능 |
| 7 | `PUB_*` 배선 + C 계열 재탐색 (M2 수축 승격) | 6 | ❌ **부결** — §10, 게이트 통과했으나 감사에서 위양성 |
| 8 | 안정성 관문(`maxTrainValidationGap`) 도입 | 7의 위양성 | ✅ 완료 (§10) — 해당 후보 재판정 부결 |
| 9 | D 계열(일정·맥락) 잔차 관문 | 발표 배당 | ❌ **부결** — §11, 유의미 표본 전 구간 ROI 음수 |
| 10 | **목표 재정의 — 시장 기대 대비 초과수익** | §11 소진 | ✅ 완료 (§12) — 위양성 0, `WIN_RATE_ODDS` 단서 확보 |
| 11 | `WIN_RATE_ODDS` 표본 확대 후 재판정 | 10 | ▶ 다음 |

각 단계는 `/algo-search` 한 이터레이션(분류→구현→스윕→판정)으로 수행하고 원장에 남긴다.

---

## 7. 관문 1·2 실행 결과 (2026-08-16)

유니버스: `야구 승1패` × KBO/NPB/MLB. 전체 2,614경기 중 3면 배당 보유 2,572경기, 116일
(260421~260815). 학습 컷 = `round(116 × 0.7) = 81`번째 일자 → **`YMD ≤ 260711`, 1,860경기**
(`BacktestSplit.byRatio`와 동일 기준). 모든 수치는 학습 구간만 사용.

### 관문 1 — 마진: 유효 마진 12~15% 실재, 단 저장 컬럼으로는 "측정" 불가

- 저장 컬럼 오버라운드: **전 행 정확히 15.0%** (min 14.80, max 15.18). 이것은 측정값이 아니라
  `BaseballResultDivBackfillService`의 가정(1.15)이 되돌아온 것 — 3면 배당은 실현 결과의
  배당(`TOTAL_DIV`)만 실측이고 나머지 두 면은 승패 마켓 비율로 역산한 **추정치**다.
- 독립 실측 (실측 `TOTAL_DIV`만 사용, 결과별 무차별 베팅 ROI): 승 **−15.5%**, 1 **−11.7%**,
  패 **−12.3%**. → 넘어야 할 다리당 허들은 설계 가정대로 실재하며, 실측 배당 기준으로
  양(+)인 무차별 구간은 없다.

### 관문 2 — 캘리브레이션: +구간은 발견됐으나 전부 아티팩트 판정

십분위 테이블(홈/무/원정 3방향)에서 `actual − implied ≥ +2%p` 구간이 다수 나왔다
(홈 implied 0.2~0.3 구간: gap +27%p, ROI +75.8% / 원정 0.4~0.5: +24.6%p / 원정 0.5+:
+41.7%p, 적중률 95%). **그러나 이는 시장 신호가 아니라 백필 배당의 차등 측정 편향이다.** 근거:

1. 실현된 면만 실측 배당이고 비실현 면은 추정치 → 십분위 **소속 자체가 결과와 상관**된다
   (예: 홈 승리 행의 `HOME_DIV`는 실측 3면 배당이라 낮은 내재확률 구간에 몰리고, 홈 패배 행의
   `HOME_DIV`는 추정치라 중간 구간에 몰림 → 낮은 구간의 적중률이 기계적으로 부풀려짐).
2. 결과별 평균 내재확률이 평탄/역전: `avg(implied_home)` = 승 0.376 / 1 0.380 / 패 0.387.
   실제 시장이라면 이긴 쪽의 사전 확률이 유의하게 높아야 한다 — 저장된 홈 내재확률은
   판별력이 사실상 0이다.
3. 원정 0.5+ 구간 적중률 95%(n=40), 무배당 최저 구간 적중 0/130 같은 수치는 실제 북메이커
   시장에서 존재할 수 없다.

참고 — 결과 의미 실증: `승` = 홈 2점차+ 승, `패` = 원정 2점차+ 승, `1` = 1점차 경기(동점 16건 포함).

### 판정 및 설계 결정 D2

- **2단계 M3(배당 구조): 부결.** 배당의 "모양"을 읽는 계열인데 모양 자체가 합성이다 —
  오버라운드가 전 행 1.15 고정이라 `maxOverround` 노브는 죽어 있고, `probLow/probHigh`
  구간 선택은 위 아티팩트를 그대로 줍는다. 표본 역산(§3-4)은 부결로 생략.
- **설계 결정 D2**: `HOME_DIV/DRAW_DIV/AWAY_DIV`를 **선택 신호**로 읽는 알고리즘의 백테스트
  수치는 신뢰할 수 없다 (선택은 추정 배당, 정산은 실측 배당 → 방향 의존 편향; 예컨대 원정
  고내재확률 추종은 가짜 +35~55% ROI가 나온다). 이후 탐색이 이런 후보에서 목표 달성을
  보고하면 **아티팩트 수확을 먼저 의심**할 것. 해제 조건: 원시(발표된) 3면 배당 데이터 확보.
- **M1 함의**: 시장 괴리(edge) 계산이 같은 컬럼을 읽으므로 동일하게 오염된다. 3단계는
  "피타고리안 − 시장 내재확률 괴리"가 아니라 **폼/점수 기반 신호로만 선택하고 배당은
  정산에만 쓰는** 변형(순수 피타고리안 랭킹 + 저노출 배당 필터 최소화)으로 재설계해야 한다.
  정산은 항상 실현 면의 실측 배당으로 이루어지므로, 선택이 배당 컬럼과 독립이면 백테스트는
  편향되지 않는다.

---

## 8. M1(D2 재설계) 사전 검증 결과 (2026-08-16) — 부결

D2 제약을 반영한 M1 변형(피타고리안 폼으로만 선택, 배당은 실측 `TOTAL_DIV` 정산에만 사용,
계열 재분류: C→B 득실 기반 랭킹)을 §3 관문에 따라 구현 전에 데이터로 검증했다.
학습 구간(`YMD ≤ 260711`)만 사용, 중복 키(COND)는 최소 id 유지.

검증 방식: 팀별 직전 N경기 득실합으로 피타고리안 기대승률(RS^k/(RS^k+RA^k))을 계산하고,
홈−원정 갭 구간별로 우세측(갭>0→승, <0→패) 베팅의 적중률·ROI를 측정.
`num ∈ {10,20,30}`, `k ∈ {1.8, 2.1}`, 수축(shrink 10) 포함 — M2 수축도 함께 판정됨.

| 검증 항목 | 결과 |
|---|---|
| 갭↑ → 우세측 적중률↑ (단조성) | **없음** — 전 구간 34~43%에서 평평, 갭 최대 구간이 최소 구간과 동급 |
| 우세측 베팅 ROI | **전 구간·전 파라미터 음수** (−2.4% ~ −27%, 대부분 마진 수준 −8~−22%) |
| 역베팅(열세측) 변형 | 전부 음수 (−13% ~ −26%) |
| 접전(갭<0.03) → 1점차('1') 베팅 변형 | 최고 +1.1% (n=126) 단일 셀, 이웃 구간 전부 음수 — 노이즈 |

목표 역산: `combinedN=2`로 검증 +10%를 내려면 다리당 약 +4.9%가 필요한데, 관측된 다리당
ROI는 최선의 셀도 +1.1%(노이즈)다. 격차가 자릿수 수준이라 스윕으로 메울 수 없다.

**판정: 구현·스윕 없이 부결.** (`algo-search` 규칙 — "데이터에 신호의 흔적조차 없으면
구현하지 않는다". 원장은 탐색 파이프라인 전용이므로 이 부결은 원장이 아니라 여기 기록한다.)

### 계열 전수 소진 — 탐색 중단 보고

| 계열 | 상태 |
|---|---|
| A 시장 추종 | 원장 5건 미달 — 구조적으로 마진만큼 짐 |
| B 폼 필터 | 승률 기반(원장 5건 미달) + 득실 기반(본 절 부결) |
| C 가치·괴리 | 원장 4건 미달 + D2로 신호 자체가 오염 (§7) |
| D 일정·맥락 | 미구현 — 효과 크기 < 마진 12~15%로 단독 달성 불가 (§M6, 문헌 근거) |
| E 배당 구조 | §7 부결 — 배당 컬럼이 합성이라 검증 불가 |
| F 조합 구성 | 단독 계열 아님 — 생존 신호 없어 평가 대상 없음 |

현재 스키마·데이터로 도달 가능한 모든 계열이 소진됐다. 남은 선택지는 코드가 아니라
**데이터 또는 목표**다: ① 원시(발표된) 3면 배당 확보 → D2 해제, C/E 계열 재개,
② 선발투수·구장 등 신규 피처 확보 → D 계열 재개, ③ 목표치 재검토(현 12~15% 마진 시장에서
+10%는 다리당 +5% 엣지를 요구 — 문헌상 스포츠 베팅 시장에서 예외적으로 큰 값).

---

## 9. 원시 발표 배당 확보 — D2 해제 (2026-08-16)

§7·§8의 병목이 "배당 컬럼이 합성"이라는 데이터 문제였으므로, ①번 선택지를 실행했다.

### 확보 방법

와이즈토토 프로토 승부식 회차 페이지(`wisetoto.com`, 2026년 46~96회차 = DB 적재 전 범위)에서
**게시된 3면 배당**을 수집했다. 회차 목록 엔드포인트(`/util/gameinfo/get_proto_list.htm`)는
직접 호출이 차단되어 브라우저 세션으로 페이지를 순회했다. `승①패` 유형 행만 채택하고
핸디캡·언더오버·홀짝·전반 마켓은 제외, 취소 경기(1.00/1.00/1.00)는 버렸다.

### 검증 — 조인과 교차검증

| 항목 | 결과 |
|---|---|
| 조인 키 | (YMD, TM, 리그, 홈, 원정) + 팀 약칭→DB 팀명 부분수열 매핑 |
| DB 승1패 2,614행 중 매칭 | **2,611행 (99.9%)** — 미매칭 3건은 MLB 서스펜디드 추정 |
| **실현면 교차검증** | **2,611 / 2,611 (100%)** — 실현된 면의 발표 배당 = `TOTAL_DIV` |
| 다중 판매 인스턴스 | 101건 (같은 경기가 인접 회차에 재판매되며 배당이 갱신) → 회차 + `TOTAL_DIV` 일치로 해소 |

교차검증 100%가 핵심이다. 실현면 배당은 DB가 독립적으로 보유한 실측값이므로, 수집한 3면 중
한 면이 항상 그것과 일치한다는 것은 조인과 수집이 모두 옳다는 강한 증거다.

### 적재

- 컬럼 추가: `PUB_HOME_DIV` / `PUB_DRAW_DIV` / `PUB_AWAY_DIV`. 이때는 승1패 2,611행에만
  적재했고, 전용 ALTER 스크립트와 TSV 데이터 파일로 반영했다.
  > **정정 (2026-08-17).** 이후 **8개 시장 전부**에 발표배당이 적재됐다(결측 0건, §16). 스키마의
  > 정본은 `db/ddl/schema.sql` 하나이며, 소진된 ALTER 스크립트와 TSV는 삭제됐다.
- **기존 `HOME_DIV`/`DRAW_DIV`/`AWAY_DIV`는 그대로 둔다** — 백필과 발표값의 차이가 D2 판정의
  근거 자체이므로 덮어쓰지 않는다. 백필 오차 실측: 홈 상대오차 6.1%, 원정 6.2%,
  **무 11.9%**(중앙값 0.31) — 무 배당 추정이 가장 나빴다.
  > **정정 (2026-08-17).** 이 세 컬럼은 이후 **제거됐다.**
  > 위 오차 수치는 이 시점에 이미 측정돼 여기 기록됐으므로 정보는 남지만, **재도출은 불가능하다.**
  > 제거 이유는 D2가 §10에서 해제된 뒤 이 컬럼들이 선택 로직에서 아무도 읽지 않으면서도 두 가지
  > 함정으로만 남았기 때문이다: 알고리즘 작성자가 `game.homeDiv()`에 손을 뻗으면 컴파일되고 조용히
  > 틀리며, 발표 배당 적재 담당이 `PUB_*` 대신 여기 넣으면 에러 없이 전 경기가 스킵된다. 백필을
  > 수행하던 `BaseballResultDivBackfillService`와 `POST /api/baseball-results/backfill-divs`,
  > 그리고 그 버튼만 있던 관리자 화면도 함께 삭제했다.

### 관문 1·2 재실행 (원시 배당, 학습 구간 1,893경기)

- **관문 1 — 마진**: 평균 오버라운드 **+15.29%** (중앙 15.0%, 범위 14.77~16.42%).
  백필의 1.15 가정은 평균적으로는 맞았다. 다만 **분산이 거의 없다**(전 행이 0.15 버킷).
- **관문 2 — 캘리브레이션**: 홈/무/원정 3방향 십분위 전부에서 `actual − implied`가 ±3%p 이내로
  붙어 있고, **표본이 유의미한 모든 구간의 ROI가 음수**(−6.6% ~ −29%). 최선이 홈 내재확률
  0.2~0.3 구간(n=419, gap +2.1%p, ROI **−6.6%**) — 갭은 +2%p를 넘지만 마진을 못 넘는다.
- **판별력(§7에서 백필이 낙제한 테스트) 통과**: `avg(implied_home)`이 홈승 0.371 > 1점차 0.359 >
  원정승 0.345로 **단조**. 원정 쪽도 대칭으로 0.355 < 0.366 < 0.382. 이제 배당이 결과를
  실제로 예측한다 — §7의 역전 현상은 백필 아티팩트였음이 확정됐다.

### 판정

- **설계 결정 D2 해제.** 단, 해제 조건은 "`PUB_*` 컬럼을 쓸 것"이다. 기존 `*_DIV` 컬럼을 선택
  신호로 읽는 코드는 여전히 오염돼 있다 — **알고리즘·백테스트가 `PUB_*`를 읽도록 바꾸기 전까지
  D2는 실질적으로 유효**하다. (`MarketEdgeSlipAlgorithm`의 de-vig, `PickBacktester`의 정산 포함)
- **C 계열(가치·괴리) 재개 가능.** §7에서 "신호 자체가 오염"이라 판정 불가였던 것이 이제
  측정 가능해졌다. 다만 관문 2가 말하는 것은 **배당 자체는 잘 캘리브레이션돼 있다**는 것이므로,
  C 계열의 승산은 "배당이 틀린 구간 찾기"가 아니라 **배당이 모르는 정보를 우리가 갖는 것**에 달렸다.
- **E 계열(배당 구조)은 여전히 부결.** 오버라운드가 14.8~16.4%로 사실상 상수여서
  `maxOverround` 노브가 판별력을 갖지 못한다.

### 다음 단계

1. `PUB_*` 컬럼을 도메인/JPA/백테스트 경로에 반영 (선택·정산 모두 발표 배당 기준으로 통일).
2. 기존 원장 14건은 **오염된 배당으로 산출된 값** — 재실행 전까지 비교 기준으로 쓰지 말 것.
3. 그 위에서 C 계열(`MARKET_EDGE`) 재탐색 + M2 수축을 `ParamSpec`으로 승격.

→ 셋 다 §10에서 수행.

---

## 10. `PUB_*` 배선 + C 계열 재탐색 (2026-08-16) — 목표 게이트는 통과, **감사에서 부결**

### 배선 (D2 완전 해제)

배당을 읽는 유일한 통로를 `BaseballResult.publishedOdds()` → `ThreeWayOdds`로 만들었다.
`formIndex()`가 미래 참조를 구조적으로 막는 것과 같은 방식이다 — 각 알고리즘 작성자가 "발표
배당만 쓴다"를 기억하는 규칙이 아니라, 그 통로 말고는 배당을 얻을 방법이 없게 했다.
**백필 컬럼으로의 폴백은 의도적으로 없다.** 폴백이 있으면 D2가 조용히 되살아난다.

| 대상 | 변경 |
|---|---|
| `ThreeWayOdds` (신규) | de-vig 1곳으로 통합 — `overround()`, `impliedHome/Draw/Away()`, `impliedDecisive()` |
| `BaseballResult` | `pubHomeDiv/pubDrawDiv/pubAwayDiv` + `publishedOdds()`. 백필 3컬럼은 javadoc에 "선택 신호 금지" 명시 |
| JPA 엔티티·어댑터·응답 DTO | `PUB_*` 3컬럼 매핑 |
| `FAVORITE`·`WIN_RATE_ODDS`·`MARKET_EDGE` | 선택 시 `publishedOdds()`만 사용, 없으면 해당 경기 스킵 |
| `MARKET_EDGE` | `FORM_SMOOTHING = 5.0` 상수 → `shrinkStrength` `ParamSpec` (M2 수축 승격, 0~45) |
| 세 알고리즘 전부 | `combinedN` 탐색 범위 2~3으로 축소 (설계 결정 D1을 코드에 반영) |

정산(`PickSettlement`)은 바꿀 필요가 없었다. `TOTAL_DIV`는 실현면의 실측 배당이고 §9에서 그것이
발표 배당과 100% 일치함을 확인했으므로, **선택과 정산이 이미 같은 시장을 인용한다.**

`MARKET_EDGE` 격자는 4×2×5×3×4 = **480점(전수)**. 예산 480으로 실행해 랜덤 샘플링을 피했다.

### 탐색 결과 (260421~260815, train 260421~260711 / validation 260712~260815, seed 42)

| 알고리즘 | 파라미터 | 학습 ROI | **검증 ROI** | 검증 조합 |
|---|---|---|---|---|
| `MARKET_EDGE` | `combinedN=2, edgeThreshold=0.05, minOdds=1.4, num=30, shrinkStrength=0` | −25.4% | **+33.2%** | 163 |
| `MARKET_EDGE` | `edgeThreshold=0.02, minOdds=1.4, num=30, shrinkStrength=0` | −23.7% | +20.0% | 268 |
| `WIN_RATE_ODDS` | `combinedN=2, num=15, x=2.2, y=2.9` | −5.9% | −0.7% | 122 |
| `FAVORITE` (기준선) | `combinedN=3` | −29.8% | −22.2% | 223 |

파이프라인 판정은 `goalAchieved = true`였다. **그대로 보고하지 않았다** — §7이 남긴 규칙이
"목표 달성 보고 시 아티팩트 수확을 먼저 의심하라"이고, 학습 −25.4% → 검증 +33.2%의 58%p 순방향
격차는 그 자체로 경보다. 파라미터가 선택된 구간에서 25% 잃은 설정이 검증에서 33% 벌었다면,
선택이 옳았다는 뜻이 아니라 **검증 구간이 그 계열에 유리했다**는 뜻일 수 있다.

### 감사 — 우승 후보를 소구간별로 재측정

동일 파라미터를 8개 소구간에서 다시 측정했다 (파라미터 선택 아님, 고정 설정의 재측정).

| 구간 | 조합 | 적중 | ROI | 평균 당첨배당 |
|---|---|---|---|---|
| 04/21–05/11 | 159 | 20 | −24.1% | 6.03 |
| 05/12–05/31 | 112 | 13 | −2.8% | 8.37 |
| 06/01–06/15 | 79 | 5 | −50.2% | 7.86 |
| 06/16–06/30 | 71 | 6 | −32.6% | 7.98 |
| 07/01–07/16 | 61 | 5 | −28.4% | 8.73 |
| 07/17–07/31 | 73 | 11 | −7.4% | 6.14 |
| **08/01–08/08** | 42 | 7 | **+41.7%** | 8.50 |
| **08/09–08/15** | 35 | 7 | **+127.3%** | 11.37 |
| **전체** | **632** | **74 (11.7%)** | **−10.3%** | 7.66 |

**8구간 중 6구간이 손실이고, 전체 기간 ROI는 −10.3%다.** 검증 구간의 +33.2%는 전부 마지막
17일에서 나왔다.

유의성: 전체 적중률 11.71%를 참값으로 두면 검증 163조합의 기대 적중은 19.1건(sd 4.1)이고
실제는 26건 — **+1.68σ**. 8월 15일까지 구간도 +1.77σ. **2σ에도 못 미친다.** ROI가 극적으로
보이는 이유는 적중이 많아서가 아니라 그 구간의 당첨이 우연히 고배당(11.37)에 몰렸기 때문이다.
손익분기 적중률은 13.05%인데 전체 실적은 11.71%로, 정확히 마진만큼 부족하다.

### 판정

- **C 계열 재탐색: 부결.** 발표 배당으로 정직하게 측정한 `MARKET_EDGE`의 실적은 −10.3%이며,
  이는 §9 관문 2의 결론(시장이 잘 캘리브레이션돼 있다)과 정확히 일치한다. 폼(승/패 승률)은
  배당이 모르는 정보를 담고 있지 않다.
- **M2 수축: 효과 없음.** 상위 후보 대부분이 `shrinkStrength = 0`(수축 없음)을 골랐고,
  수축 45를 쓴 후보는 검증 −1.5%였다. 수축의 유무가 결과를 바꾸지 못한다 — 애초에 폼 추정의
  분산이 문제가 아니라 폼 신호 자체에 정보가 없기 때문이다.
- **원장 항목 `7abe3151…`은 위양성으로 기록한다.** 원장은 append-only이므로 삭제하지 않되,
  리더보드 최상단의 "달성"은 본 절의 감사 결과와 함께 읽어야 한다.

### 목표 게이트의 구조적 결함 → 안정성 관문 도입

`ExperimentGoal`은 검증 구간의 **합계**만 봤다. 그래서 마지막 2주의 고배당 연속 적중 하나로
게이트를 통과할 수 있었고, 실제로 통과했다. 목표치를 낮추는 것이 아니라 **조이는** 변경이므로
곧바로 반영했다 (`research.goal.max-train-validation-gap: 0.45`).

- **`maxTrainValidationGap`** — `|학습 − 검증|` 수익률 상한. 두 구간이 크게 어긋나면 엣지가 아니라
  꼬리의 운이다. 임계 0.45의 근거: 검증 163조합·학습 469조합, 평균 당첨배당 7.7·적중률 12%에서
  수익률 차이의 표준오차가 약 0.23이므로 **0.45 ≈ 2σ**. 이보다 좁히면 진짜 엣지도 걸러낸다.
- **`minProfitableSegments`** (선택, 기본 미설정) — 검증 구간을 4등분했을 때 수익 구간 수 하한.
  `BacktestMetrics`가 `segmentCount`/`profitableSegmentCount`를 항상 계산해 보고한다.

재판정 결과 — 문제의 후보는 이제 **부결**된다:

| 검사 | 기준 | 실측 | 판정 |
|---|---|---|---|
| 수익률 | ≥ 0.10 | 0.3315 | PASS |
| 최소 조합 수 | ≥ 100 | 163 | PASS |
| 최소 베팅일 수 | ≥ 20 | 43 | PASS |
| **학습·검증 격차** | **≤ 0.45** | **0.5854** | **FAIL** |

**중요한 부수 발견: 구간 분할 검사는 이 후보를 걸러내지 못한다.** 검증 구간(07/12~08/15) 내부만
4등분하면 **4/4 전부 수익**이기 때문이다 — §10 상단의 월별 감사에서 손실로 나온 구간들은 대부분
검증 구간 *이전*이었다. 반대로 학습 구간은 **0/4**다. 즉 "검증 구간 안에서의 안정성"은 이 위양성에
대해 무력했고, 실제로 일을 한 것은 **두 구간의 불일치**를 보는 검사다. 그래서 격차 검사만
기본 활성화하고 구간 검사는 선택으로 남겼다.

이것이 일반 교훈이다: 홀드아웃 구간 하나가 아무리 깨끗해 보여도, **학습 구간과 말이 맞지 않으면
그 구간이 특이했던 것**이다.

### 계열 현황 갱신

| 계열 | 상태 |
|---|---|
| A 시장 추종 | 부결 — `FAVORITE` 발표 배당 기준 검증 −22.2% |
| B 폼 필터 | 부결 — 승률 기반 검증 −0.7%(최선), 득실 기반 §8 부결 |
| C 가치·괴리 | **부결(본 절)** — 오염 제거 후 실측 −10.3%, 마진과 일치 |
| D 일정·맥락 | 미구현 — 신규 피처(선발투수·구장) 확보 필요 |
| E 배당 구조 | 부결 — 오버라운드 14.8~16.4%로 사실상 상수 (§9) |
| F 조합 구성 | 평가 대상 없음 — 생존 신호 부재 |

D2 해제로 재개할 수 있었던 C·E 두 계열을 정직한 데이터로 소진했다. 남은 선택지는 §8과 동일하되
①이 소진된 상태다: **② 신규 피처(선발투수·구장·불펜 등) 확보 → D 계열**, 또는 **③ 목표치 재검토**.

---

## 11. D 계열(일정·맥락) SQL 관문 (2026-08-16) — 부결

§8은 D 계열을 "효과 크기 < 마진"이라는 **문헌 근거만으로** 미구현 처리했다. 이제 발표 배당이
있으므로 실측할 수 있다 — 그리고 실측은 구현보다 훨씬 싸다.

먼저 `match_schedule`을 확인했다. **0행이고 추가 피처 컬럼도 없다** — 무료로 얻을 것은 없었다.
따라서 `baseball_result`만으로 파생 가능한 일정 피처를 대상으로 했다.

**측정 방식이 핵심이다: 적중률이 아니라 잔차를 본다.** 일정 정보는 북메이커도 아는 공개
정보이므로, 질문은 "휴식일이 승패를 예측하는가"가 아니라 "**배당이 이미 반영한 것 이상을**
예측하는가"다. 그래서 구간별 `실제 홈승률 − de-vig 내재 홈확률`을 본다. 시장이 완전히 반영하고
있으면 잔차는 모든 구간에서 0 근처다. 학습 구간(`YMD ≤ 260711`) 1,893경기.

| 피처 | 최선 구간 (n ≥ 300) | 잔차 | 홈베팅 ROI |
|---|---|---|---|
| 휴식일 차이(홈−원정) | `+0` (n=1,721) | −0.61%p | −14.4% |
| 홈팀 휴식일 | 2일 (n=324) | +5.05%p | **−2.7%** |
| 7일 경기수 차이 | −1 (n=313) | +4.14%p | **−1.5%** |
| 경기 시각 | 주간 (n=1,381) | −0.68%p | −14.6% |
| 리그 | NPB (n=360) | +3.99%p | **−1.0%** |

**표본이 유의미한 모든 구간의 ROI가 음수다.** 잔차가 +4~5%p로 보이는 구간들(홈휴식 2일,
저밀도, NPB)조차 손익분기에 못 미친다 — 마진 15.3%를 못 넘는다.

유의성도 없다. n=324에서 비율의 표준오차는 2.7%p이므로 +5.05%p는 **+1.9σ**이고, 이번 관문에서
비교한 구간은 약 35개다. 순전한 우연으로도 1.9σ급이 2개쯤 나오는 규모다.

부수 사실: 휴식일 차이는 **91%가 `+0`**(양 팀 모두 전날 경기)이다. 야구 일정 구조상 휴식일
비대칭 자체가 거의 발생하지 않아, 이 피처는 애초에 변별할 표본을 만들지 못한다.

### 판정

- **D 계열 부결.** §8의 사전 판단이 실측으로 확인됐다.
- 이로써 **A~F 여섯 계열 전부가 정직한 데이터 위에서 측정·소진**됐다. 다섯 번 서로 다른 각도로
  물었고 답은 매번 같다: **이 시장의 배당은 우리가 가진 정보를 이미 전부 반영하고 있다.**

### 남은 두 갈래

| 선택지 | 내용 | 비용 | 사전 확률 평가 |
|---|---|---|---|
| ② 선발투수 피처 | MLB StatsAPI(`statsapi.mlb.com`, 무료·공개)에 2026 시즌 `probablePitcher`가 살아 있음을 확인. MLB는 학습 표본의 64%(1,207경기) | 날짜 116회 + 투수 게임로그 호출, 팀명 한↔영 매핑 | **낮음** — 선발투수는 북메이커가 가장 확실히 반영하는 변수다. 시장이 모르는 정보가 아니다 |
| ③ 목표치 재조정 | +10%는 마진 15.3% 시장에서 다리당 +5% 엣지를 요구. 관문 1·2가 그런 엣지의 부재를 직접 보여줬다 | 없음 | — 사용자 결정 사항 (`research.goal`은 임의 변경 금지) |

③을 택할 경우 검토할 만한 재정의: **절대 수익률 대신 상대 목표** (`FAVORITE` 기준선 대비 개선폭),
또는 **손실 최소화 목표**(예: ROI ≥ −5%). 후자는 "마진을 얼마나 덜 내는가"를 묻는 것이며,
관문 2가 보여준 시장에서는 이쪽이 답이 존재하는 질문이다.

→ 사용자가 ③을 선택. §12에서 재정의.

---

## 12. 목표 재정의 — 시장 기대 대비 초과수익 (2026-08-16)

### 왜 절대 수익률이 목표로 부적절했나

절대 ROI는 **"돈을 벌었나"와 "정보가 있었나"를 한 숫자에 섞는다.** 마진 15.3% 시장에서 앞의
답은 거의 항상 '아니오'이고, 그건 뒤의 질문에 대해 아무것도 말해주지 않는다. 그런데 우리가
답을 구할 수 있는 질문은 뒤쪽이다.

기준선은 배당만으로 정확히 계산된다. de-vig 항등식상 어느 면을 고르든 한 다리의 기대수익은
`p_s × odds_s = 1/O` 로 **동일**하다(선택한 면과 무관). 실측 `O = 1.1529` → **−13.26%**.
학습 구간 실측이 이를 확인한다:

| | 값 |
|---|---|
| 이론 무기술 기대수익 `1/O − 1` | **−13.26%** |
| 홈 무조건 베팅 | −13.50% |
| 무(1점차) 무조건 베팅 | −10.79% |
| 원정 무조건 베팅 | −13.78% |

"기술 0"의 성적표가 사전에 정확히 정해져 있으므로, 그것을 빼고 남는 것이 곧 전략의 기여분이다.

### 세 번의 설계 수정 — 매번 같은 결함

목표를 초과수익으로 바꾼 뒤에도 파이프라인은 두 번 더 "달성"을 반환했고, 두 번 다 원인은
**표본 크기를 보지 않는 임계값**이었다.

1. **조합(슬립) 단위 → 다리(leg) 단위.** 첫 실행에서 `FAVORITE`가 3다리 **+12.0%**, 2다리
   **−14.3%**로 나왔다. 둘은 **같은 경기를 그대로 베팅하고 묶는 방식만 다르다.** 진짜 엣지는
   묶는 방식에 따라 부호가 뒤집힐 수 없다 — 3다리 조합의 분산이 너무 커서 100여 조합으로는
   신호가 묻혔을 뿐이다. 다리 단위는 표본이 2~3배이고, 조합 기준선이 다리 기준선의 곱이므로
   묶기의 영향이 **구조적으로 0**이다.
2. **고정 임계값 → t값.** 다리 300개면 노이즈만으로 ±7%p, 900개면 ±4%p다. 같은 +5%p가 표본에
   따라 전혀 다른 의미이므로 `min-excess-return`만으로는 관문이 될 수 없다. `t ≥ 2.0`을 함께
   걸었다 — 자주 베팅해 작은 엣지를 입증하든, 드물게 베팅해 큰 엣지를 보이든 둘 다 길이 열린다.
3. **절대 격차 → σ 격차.** 안정성 관문도 같은 결함이 있었다. `max-train-validation-gap: 0.17`은
   §10 후보의 표준오차 기준 2σ였는데, 표준오차가 더 작은 다른 후보에게는 2.2σ여서 **통과해버렸다**
   (격차 0.1677 < 0.17). `max-train-validation-gap-t-stat: 2.0`으로 대체했다.

최종 목표(`application.yaml`, 요청으로 변경 불가):

```yaml
min-excess-return: 0.02               # 실용 하한 — 유의해도 2%p 미만은 성과로 부르지 않음
min-excess-t-stat: 2.0                # 유의성 — 노이즈로는 넘을 수 없게
min-slip-count: 100 / min-betting-day-count: 20
max-train-validation-gap-t-stat: 2.0  # 두 구간은 노이즈로 설명되는 범위에서만 어긋나야 함
```

랭킹(`ObjectiveScore`)도 다리 단위 초과수익으로 전환했다. 목표와 다른 기준으로 후보를 고르면
탐색이 목표를 향하지 않는다.

### 재정의 후 첫 전수 탐색 (962후보, `MARKET_EDGE` 480점 전수, seed 42)

**`goalAchieved: false` — 위양성 없음.**

| 알고리즘 | 학습 다리 | 학습 초과 (t) | 검증 다리 | 검증 초과 (t) | 불일치 |
|---|---|---|---|---|---|
| `MARKET_EDGE` (최고) | 930 | −0.8% (−0.18) | 318 | **+25.7%** (3.19) | **2.90σ** ❌ |
| `MARKET_EDGE` 외 3건 | 466~1,260 | −0.3~−1.0% | 146~480 | +15.8~+28.5% | 2.20~2.59σ ❌ |
| `WIN_RATE_ODDS` (최고) | 741 | **+5.1%** (1.16) | 315 | **+11.5%** (1.70) | **0.79σ** ✅ |
| `WIN_RATE_ODDS` 외 3건 | 478~963 | +5.0~+5.6% | 170~381 | +2.7~+20.1% | 0.40~1.38σ ✅ |
| `FAVORITE` | 1,758~1,810 | −1.2~−1.8% | 669~694 | −6.3~−7.1% (−1.85) | 0.98~1.30σ |

세 가지를 읽을 수 있다.

- **`MARKET_EDGE`는 창(window) 효과다.** 학습 구간 초과수익이 t = −0.07~−0.25, 즉 **정밀하게 0**인데
  (표본이 1,000다리 내외라 ±4%p로 좁다) 검증에서만 +16~29%가 나온다. 파라미터를 어떻게 잡아도
  같은 패턴이므로 파라미터가 아니라 **구간이 특이했다.**
- **`FAVORITE`는 두 구간 모두 음수다.** 시장 추종이 무기술보다 나쁘다 — 인기팀 편향
  (favorite–longshot bias)이 예상대로 나타났다. 측정 장치가 옳게 작동한다는 **좋은 검증**이기도
  하다: 정보 없는 기준선은 0 이하여야 하고, 실제로 그렇다.
- **`WIN_RATE_ODDS`만 두 구간 모두 양수다.** 모든 설정에서 학습이 +5%대로 거의 일정하고 검증도
  양수, 불일치 0.4~1.4σ. 합산 추정 **+7.0%p/다리, t ≈ 1.90**(1,056다리). **유의성 미달이지만,
  이 프로젝트에서 처음으로 노이즈가 아니라 신호처럼 보이는 결과다** — 부호와 크기가 두 구간에서
  일치하기 때문이다.

### 판정

- 목표 미달. 루프는 계속된다.
- **가장 유망한 단서: B 계열(폼 필터)의 `WIN_RATE_ODDS`.** §11까지 "부결"로 적은 것은 절대 수익률
  기준이었기 때문이다. 초과수익으로 보면 이 계열만 일관되게 양수다.
- 다음 이터레이션의 우선순위는 **새 계열 추가가 아니라 `WIN_RATE_ODDS`의 표본 확대**다. t는 표본의
  제곱근에 비례하므로, 같은 +7%p 엣지라도 다리를 1,056 → 2,300개로 늘리면 t ≈ 2.8이 되어 판정이
  가능해진다. 필터 완화(`x` 하향, `y` 상향)나 기간 확대가 그 수단이다.
  > **정정 (§15).** 이 계산은 틀렸다. 1,056다리는 두 구간 합산이지만 목표는 **검증 구간만으로** 판정한다.
  > 검증 창의 경기는 628건이 상한이고 기간 확대는 불가능(DB에 이 범위 밖 경기 0건)하므로, +7%p는
  > 완전 개방에서도 t = 1.58까지밖에 가지 못한다. 이 경로는 애초에 닫혀 있었다.
- 주의: 이 +7%p는 파라미터를 학습 구간에서 고른 뒤의 값이라 낙관 편향이 있다. 표본을 늘린 뒤
  **새 홀드아웃**에서 재확인해야 한다.

## 13. 베팅 시장 교체 — `야구 승패`(2면) 도입 (2026-08-17)

### 문제: `1`은 무승부가 아니라 1점차 경기다

`FAVORITE_MARTINGALE`의 슬립 적중률이 20%에 그쳤다. 원인은 알고리즘이 아니라 **시장 선택**이었다.
`야구 승1패`의 `TOTAL_RESULT`는 `승` = 홈 2점차 이상, `1` = **1점차 경기**, `패` = 원정 2점차 이상이다
(§6에서 이미 확인된 사실이지만, 그것이 적중률의 상한을 정한다는 점은 여기서 처음 결론으로 쓰였다).
시장 내재확률로 `1` 구간이 22~28%를 차지하므로 **우세팀 픽은 1점차 경기를 전부 잃는다.**

배당을 낮춰 적중률을 올리는 길은 막혀 있다. 발표배당 2,611건 중 어느 한 면이라도 1.50 미만인 경기는
10건(0.38%), 최저가는 1.36이다. 재고 자체가 없다.

`야구 승패`에는 이 중간 구간이 없다. **같은 팀을 그대로 픽하되 1점차 승리가 적중이 된다.**

### 2면 오버라운드 실측 = 1.13629 (상수)

사용자가 제공한 와이즈토토 `야구 승패` 발표배당 24경기(2026-08-14~15, MLB 15 / NPB 6 / KBO 3)에서
`1/승 + 1/패`를 계산했다.

| | 값 |
|---|---|
| 평균 | **1.13629** |
| 범위 | 1.13516 ~ 1.13769 |
| 표준편차 | ≈ 0.0007 |

리그·배당대와 무관하게 상수다(1.21/3.23 → 1.1360, 2.56/1.34 → 1.1369). 3면의 1.15와 같은 성격의
고정 하우스 마진이다. 무기술 레그 기대수익은 `1/O − 1`이므로 **3면 −13.19% → 2면 −11.99%**,
시장을 바꾸는 것만으로 +1.2%p가 붙는다.

`BaseballResultDivBackfillService`가 실현 배당에서 역산해 쓰던 1.14는 약 0.4% 높았고, 실측값으로 교체했다.

> **정정 (2026-08-17, §16).** "상수"가 아니었다. 승패 발표배당 2,676행 전수로 재측정하면 합산
> 1.1412이고, 260421~260712 약 1.143 → 260713~260817 약 1.136으로 **이동한다.** 위 24경기는
> 08-14~15 표본이라 후반 구간값을 잡은 것이었다. 0.7%p 이동은 쫓고 있는 엣지와 같은 크기이므로
> 마진은 경기별로 계산한다(`TwoWayOdds.overround()`). 아래 −11.99%도 합산 기준 −12.4%로 읽을 것.

### 유추 공식과 그 검증

> **정정 (2026-08-17, §16.1).** 이 절의 추정기는 **삭제됐다.** 8개 시장 전부에 발표배당이 적재돼
> 승패 행이 자기 가격을 갖게 됐고, 추정기를 실측과 대조하니 가격 오차는 1.5~1.7%로 작았지만
> **우세팀 일치율이 0.8558** — 7경기 중 1경기에서 다른 팀을 지목했다. §13~§15의 승패 수치는 전부
> 그 위에서 측정된 것이다. 아래는 그때의 설계 기록으로만 남긴다.

`야구 승패` 행에는 `PUB_*`가 없다. 이긴 쪽 `TOTAL_DIV`만 있고, 그것을 선택·사이징에 쓰면 D2 그대로다.
그래서 **같은 경기의 3면 발표배당에서 2면 가격을 유도**한다 — 입력이 전부 경기 전 공표값이므로 누수가
구조적으로 없다.

```
p2_home = impliedHome + s · impliedDraw          (s = 1점차 경기 중 홈 승리 비율)
p2_away = impliedAway + (1 − s) · impliedDraw
odds2   = 1 / (p2 × 1.13629)                     (2자리 반올림)
```

`GET /api/research/two-way-odds-check`로 실현면 `TOTAL_DIV`(= 이긴 쪽의 실제 발표가)와 대조해 검증했다.
2,369경기 중 2,349건(99.2%)이 승패 행과 매칭됐고, 그중 2,332건을 비교했다.

| `s` | 평균 절대오차 | 승 실현 편향 | 패 실현 편향 |
|---|---|---|---|
| 0.50 | 5.17% | +5.59% | −4.65% |
| 0.55 | 2.70% | +2.77% | −2.00% |
| **0.60** | **1.60%** | **+0.10%** | **+0.79%** |
| 0.65 | 3.17% | −2.43% | +3.77% |
| 0.70 | 5.83% | −4.85% | +6.91% |

**두 면의 편향이 동시에 0을 지나는 지점이 0.60이다.** 즉 홈팀이 1점차 경기의 약 60%를 이긴다 —
홈 어드밴티지가 접전에 몰려 있다는 뜻이다. 코인플립 가정 0.5는 측정 가능하게 틀렸다.

중요한 구분: 이 보정의 대상은 **결과가 아니라 발표가격**이다. 결과는 식에 들어가지 않는다. 다만 관측
가능한 것이 이긴 쪽 가격뿐이라 표본이 결과에 조건화되어 있으므로, `oneRunHomeShare`는 상수로 박지 않고
**스윕 파라미터로 남겨 학습 구간 초과수익으로 고른다.** 이 측정은 스윕 범위의 중심만 정한다.

버킷별 캘리브레이션(예측 대 실측)은 의미 있는 표본 구간 전부에서 ±2.6%p 이내였다 — 추정기는
편향되지 않았고, 동시에 **시장은 효율적이다**(어느 배당대에서도 실측이 내재확률을 유의하게 넘지 않는다).

### 탐색 결과 (260421~260815, train 260421~260711 / validation 260712~260815, seed 42, 120후보 전수)

검증 구간 수치. 같은 경기·같은 픽·같은 정산 코드다.

| 알고리즘 | 조합 | 적중 | **적중률** | 레그 초과수익(검증) | (학습) |
|---|---|---|---|---|---|
| `FAVORITE` (combinedN=2) | 333 | 49 | 0.1471 | −0.0618 | −0.0119 |
| **`FAVORITE_2WAY`** (combinedN=2, s=0.60) | 330 | 112 | **0.3394** | +0.0006 | +0.0078 |
| `FAVORITE` (combinedN=3) | 215 | 18 | 0.0837 | −0.0547 | −0.0180 |
| **`FAVORITE_2WAY`** (combinedN=3, s=0.60) | 214 | 49 | **0.2290** | +0.0064 | +0.0168 |
| `FAVORITE_MARTINGALE` | 59 | 10 | 0.1695 | −0.0776 | −0.0477 |
| **`FAVORITE_2WAY_MARTINGALE`** (s=0.55) | 59 | 18 | **0.3051** | −0.0163 | +0.0352 |

레그 단위 적중률은 같은 경기에서 **41.4% → 56.4%**다.

두 가지가 눈에 띈다.

1. **적중률은 목표대로 올랐다.** 2게임 조합 기준 0.147 → 0.339 (2.3배), 마틴게일 0.170 → 0.305 (1.8배).
   `0.564² = 0.318`이라는 산술과 일치하고, 짧은 배당부터 고르므로 그보다 높게 나온다.
2. **레그 초과수익이 세 쌍 모두에서 약 +6.2%p 개선됐다** (−0.0618→+0.0006, −0.0547→+0.0064,
   −0.0776→−0.0163). 서로 독립인 세 비교에서 같은 크기로 나온 것은 우연이 아니라 구조적이라는 뜻이다.
   그중 +1.2%p는 마진 차이로 설명되고, 나머지 ~5%p는 favorite–longshot bias가 2면에서 덜 작동한다는
   해석이 가능하다 — 다만 아래 판정을 볼 것.

### 판정 — 미달 (`goalAchieved: false`)

최고 후보 `FAVORITE_2WAY (combinedN=3, s=0.65)`의 검증 초과수익은 **+0.76%p, t = 0.25**다.
요구치는 +2%p이고 t ≥ 2.0이므로 **양쪽 다 미달**이다. 위 2번의 "+6.2%p 개선"은 기준선 대비 상대적
개선이지 절대적으로 유의한 초과수익이 아니다. §10·§12에서 세 번 반복된 위양성이 정확히 이 지점 —
"좋아 보이는 상대 비교"를 절대 판정으로 바꿔 읽는 것 — 에서 나왔으므로 여기서 멈춘다.

**즉 이번 변경의 확정된 성과는 적중률과 시장 마진이지, 수익성이 아니다.**

### 예상과 어긋난 것: 최대 낙폭은 오히려 커졌다

사전 예측은 "적중률이 오르니 시퀀스 한도소진이 줄어 낙폭도 준다"였다. 실측은 반대였다:
`FAVORITE_2WAY_MARTINGALE` 131,771~164,103원 대 `FAVORITE_MARTINGALE` 61,110~88,928원.

이유는 마틴게일의 배수가 `O/(O−1)`이기 때문이다. 조합배당이 3.0에서 2.3으로 내려가면 배수가
1.50 → 1.77로 **커진다**. 시퀀스당 한도소진 확률은 낮아지지만(10단계 필요 → 7단계 필요), 시퀀스가
그만큼 빨리 끝나 같은 기간에 더 많은 시퀀스를 돌게 되므로 **단위 기간당 소진 횟수는 비슷해지고
각 단계의 배팅금만 커진다.** 59슬롯은 이 지표를 논하기엔 작은 표본이지만, "적중률이 오르면 낙폭이
준다"는 추론이 마틴게일에서는 성립하지 않는다는 점은 분명하다.

### 계열 현황 갱신

시장 교체는 **새 신호 계열이 아니다**(taxonomy A + F). `s = 0.5`에서 2면 우세팀은 항상 3면 우세팀과
같으므로, 픽 대상은 바뀌지 않고 정산 시장만 바뀐다(보정값 0.60에서는 근소한 원정 우세 경기가 홈으로
뒤집힐 수 있다 — 2면 시장이 실제로 홈에 더 유리하므로 옳은 거동이다).

따라서 §12의 우선순위는 유지된다: 다음은 **`WIN_RATE_ODDS`의 승패 버전**이다. 두 구간 모두 초과수익이
양수인 유일한 계열이고, 이제 그 위에 +6%p짜리 시장 교체 이득을 얹을 수 있는 배관이 있다.

## 14. B 계열 × 승패 시장 — `WIN_RATE_ODDS_2WAY` (2026-08-17)

§13의 배관 위에 §12가 지목한 계열을 올렸다. 근거는 두 이득이 **서로 독립**이라는 것이다 — 시장 교체는
정산 시장을 바꾸고, 폼 필터는 어느 면을 고를지 바꾼다.

### `x`/`y`는 이름만 같고 다른 값이다

두 임계값은 배당에 직접 비교되는데, 2면 가격은 3면보다 체계적으로 짧다. 발표배당 2,611경기에서 측정:

| | p05 | p50 | p95 |
|---|---|---|---|
| 3면 홈 | 1.78 | 2.40 | 3.65 |
| 3면 원정 | 1.76 | 2.40 | 3.40 |
| **2면 홈** | 1.38 | 1.67 | 2.19 |
| **2면 원정** | 1.47 | 1.86 | 2.43 |

3면 범위 1.4~3.0을 그대로 쓰면 임계값 대부분이 데이터 위쪽 바깥에 놓여 **필터가 아예 걸리지 않는다.**
그래서 2면판은 1.2~2.6으로 이동했다(step 0.1로 해상도는 동일). 임계값이 판단하는 대상은 **실제로
받는 가격**이어야 하므로 3면 가격이 아니라 추정된 2면 가격에 비교한다.

`oneRunHomeShare`는 0.55~0.65로 좁혔다. 전략 임계값이 아니라 가격 모델 파라미터이고 §13에서 이미
독립적으로 보정됐으며(0.60에서 오차 1.60%, 양옆 2.70%/3.17%), 9점 스윕은 격자를 9배로 불려
비교 대상인 3면판보다 탐색 밀도를 떨어뜨리기만 한다.

### 탐색 결과 (같은 창, seed 42, 알고리즘당 후보 2,000)

격자는 3면 4,624(43% 탐색) / 2면 10,800(19% 탐색)으로 **3면 쪽이 더 촘촘히 탐색됐다** — 아래 비교는
2면에 불리한 조건이다.

| | `WIN_RATE_ODDS` | `WIN_RATE_ODDS_2WAY` |
|---|---|---|
| 최고 후보 | combinedN=2, num=15, x=2.2, y=1.9 | combinedN=3, num=15, s=0.55, x=1.4, y=2.6 |
| 검증 조합 수 | **52 (실격, < 100)** | 108 |
| 검증 다리 수 | 104 | 324 |
| 레그 적중률 | 0.4615 | **0.5494** |
| 검증 레그 초과수익 | +0.3128 (t=2.45) | +0.0191 (t=0.42) |
| 학습 레그 초과수익 | +0.0696 (t=1.17) | +0.0534 (t=1.94) |

**`WIN_RATE_ODDS`의 +31.3%p·t=2.45는 결과가 아니다.** 조합 52건·적중 11건으로 표본 게이트에서
실격이고, 파이프라인이 그렇게 진단했다. §10에서 부결된 위양성과 같은 모양 — 적중이 많아서가 아니라
당첨이 우연히 고배당에 몰려 생긴 수치다. 이 프로젝트에서 세 번 반복된 실수이므로 인용하지 않는다.

주목할 것은 **2면판의 표본이 두 배 이상 크고 학습 구간이 안정적이라는 점**이다. 상위 8개 후보의 학습
레그 초과수익이 +0.0534~+0.0632 좁은 범위에 모여 있고, 검증도 전부 양수다. 특히
`combinedN=2, num=10, s=0.60`은 **검증 +0.1568 (t=1.87, 88조합/176다리), 학습 +0.0626, ROI −0.0066**로
두 구간 모두 양수이면서 손익분기 근처다 — 현재까지 이 프로젝트에서 가장 그럴듯한 후보다.

### 판정 — 미달 (`goalAchieved: false`)

최고 후보 +1.91%p (요구 +2%p), t = 0.42 (요구 2.0). 양쪽 다 미달이다.

한계는 명확히 **표본**이다. 검증 구간이 35일뿐이라 t가 자랄 수 없다. §12가 이미 계산한 대로 같은
엣지에서 다리를 1,056 → 2,300개로 늘리면 t가 2.8이 된다. 다음 이터레이션은 새 계열이 아니라
**기간 확대 또는 필터 완화로 표본을 키우는 것**이어야 한다. 최고 후보의 `y = 2.6`이 스윕 범위 상단에
붙어 있다는 것도 같은 방향을 가리킨다 — y 제약이 사실상 걸리지 않는 지점이 선택됐다는 뜻이므로,
범위를 넓히기보다 다른 축에서 표본을 늘리는 편이 낫다.

## 15. 필터 완화로 표본 확대 — **네 번째 위양성** (2026-08-17)

### 먼저: 기간 확대는 불가능하고, 검증 창에는 통계적 상한이 있다

DB에 `260421~260815` 밖의 경기는 **0건**이다(전 범위 조회로 확인). 표본을 늘리는 길은 필터 완화뿐이다.

그런데 검증 구간(260712~260815, 35일)의 경기는 **628건이 상한**이다. 필터를 완전히 열어 모든 경기를
담아도:

| 검증 다리 수 | t ≥ 2.0에 필요한 레그 엣지 | §12 기준 +7.0%p일 때 t |
|---|---|---|
| 628 (완전 개방) | **+8.9%p** | **1.58 — 미달** |

**즉 이 데이터셋에서 +7%p 수준의 엣지는 30% 홀드아웃으로는 결코 t ≥ 2.0을 만들 수 없다.**
§12가 적은 "다리를 1,056 → 2,300개로 늘리면 t ≈ 2.8"은 **두 구간 합산** 기준이었는데 목표는 검증
구간만으로 판정하므로, 그 경로는 애초에 닫혀 있었다. §12의 해당 문장은 이 정정으로 대체한다.

### 한 일

코드에 박혀 있던 `RANK_LIMIT = 5`를 `rankLimit`(3~15) 스윕 대상으로 바꿨다. 이 계열에서 유일하게
검증된 적 없는 임계값이자, 후보로 인정되는 팀 수를 직접 정하므로 표본에 가장 큰 레버다. `x`/`y` 범위도
완화 방향으로 넓혔다. 격자는 3면 15,200 / 2면 17,280으로 맞췄고 예산은 각 4,000이다.

### 결과: `goalAchieved: true` — 그리고 그것은 위양성이었다

`WIN_RATE_ODDS (combinedN=2, num=35, rankLimit=9, x=2.6, y=3.2)`가 검증 초과수익 **+16.34%,
t = 2.44, 169조합**으로 모든 게이트를 통과했다. 이 프로젝트 최초의 "달성"이다.

§10의 절차대로 우승 파라미터를 달력 소구간별로 재측정했다:

| 구간 | 다리 | 레그 적중률 | ROI | 레그 초과수익 | t |
|---|---|---|---|---|---|
| 260421~260531 | 394 | 0.3553 | −0.4619 | +0.0367 | 0.58 |
| 260601~260630 | 306 | 0.3562 | −0.2648 | **−0.0218** | −0.33 |
| 260701~260731 | 282 | 0.4291 | −0.0099 | +0.1649 | 2.25 |
| 260801~260815 | 140 | 0.4286 | +0.2643 | +0.1770 | 1.68 |

**+16.3%는 전부 7~8월에서 나왔고, 그 구간이 곧 검증 창이다.** 6월은 음수다. 레그 적중률도
0.355 → 0.429로 뛴다 — 엣지가 아니라 **국면 전환**이다. §10에서 부결된 `MARKET_EDGE`와 같은 모양이다.

원인은 세 가지가 겹쳤다.

1. **미달 직후 탐색 공간을 넓혀 재실행했다.** 시드를 바꿔 재실행하는 것과 같은 행동이다. `rankLimit`
   스윕에는 독립적 근거(미검증 상수)가 있었지만, 넓힌 직후 나온 "달성"은 그 사실을 안고 읽어야 한다.
2. **검증 창 다중검정.** 후보 10개를 검증하면 순전히 우연으로 `t ≥ 2.0`이 하나 나올 확률이 약 21%다.
3. **7~8월 국면.** 이 창에서는 어떤 우세팀 추종 규칙이든 좋아 보인다.

`min-profitable-segments: 3`을 켰다면 이 후보는 걸렸을 것이다(검증 창 내부 2/4). 다만 그 게이트는
초과수익이 아니라 ROI 기준이라 2면판(1/4)도 함께 탈락시키므로 깨끗한 판별기가 아니다. 목표는
**변경하지 않았다.** 대신 `.claude/skills/algo-search/SKILL.md`에 **"달성 시 소구간 감사 필수"**를
명문화했다 — 위양성을 두 번 다 잡아낸 것은 게이트가 아니라 이 절차였다.

### 감사가 덤으로 알려준 것: 2면판이 더 견고하다

같은 감사를 `WIN_RATE_ODDS_2WAY (combinedN=2, num=35, rankLimit=6, x=1.2, y=1.6, s=0.65)`에도 했다.

| 구간 | 다리 | 레그 적중률 | 레그 초과수익 | t |
|---|---|---|---|---|
| 260421~260531 | 252 | 0.5278 | +0.0394 | 0.69 |
| 260601~260630 | 192 | 0.6042 | +0.1085 | 1.81 |
| 260701~260731 | 180 | 0.5833 | +0.0883 | 1.41 |
| 260801~260815 | 102 | 0.5588 | +0.0424 | 0.51 |

**4/4 구간 전부 양수**이고 어느 한 구간에 몰려 있지 않다. 파이프라인 판정은 미달(검증 t = 1.35)이지만,
"달성"을 받은 3면판보다 이쪽이 신호에 가깝다. 학습·검증이 **+0.0797 / +0.0766**으로 거의 일치한다는
점도 같은 방향이다.

전 구간 합산(파라미터를 학습 구간에서 골랐으므로 **깨끗한 out-of-sample이 아니며 판정에 쓸 수 없다**):

| | 다리 | 합산 초과수익 | SE | t | 양수 구간 |
|---|---|---|---|---|---|
| `WIN_RATE_ODDS` | 1,122 | +7.05%p | 0.0364 | 1.94 | 3/4 |
| `WIN_RATE_ODDS_2WAY` | **726** | +7.02%p | **0.0319** | **2.20** | **4/4** |

두 계열이 §12의 독립 추정치(+7.0%p)와 정확히 같은 값으로 수렴한다. 그리고 2면판은 **다리 수가 35%
적은데도 SE가 더 작다** — 배당이 짧아 레그당 분산이 낮기 때문이다. 이것이 시장 교체의 세 번째 이득이다:
마진(+1.2%p)과 적중률에 더해, **같은 엣지가 더 적은 표본으로 유의해진다.**

### 판정 — 미달, 그리고 루프의 한계

- 3면판의 "달성"은 **위양성**으로 기록한다. `pick_mstr` 확정 시뮬레이션은 하지 않았다.
- 2면판은 견고해 보이지만 검증 t = 1.35로 미달이다.
- **이 데이터셋으로는 더 갈 수 없다.** 필터는 이미 열었고, 기간은 없고, 검증 창 상한(628다리)에서는
  +7%p가 t = 1.58까지밖에 못 간다. 남은 선택지는 두 가지뿐이다:
  1. **새 데이터 확보** (다른 시즌). 유일하게 정직한 길이다.
  2. train/validation 비율 조정으로 검증 창을 늘리기 — 결과를 본 뒤 바꾸는 것이므로 오염된다.
     새 홀드아웃이 없는 한 그 판정은 신뢰할 수 없다.

---

## 16. 발표 배당 전면 확보 — 추정기 폐기, 그리고 **다섯 번째 위양성** (2026-08-17)

데이터가 바뀌었다. `baseball_result`의 백필 `*_DIV` 3종이 제거되고(1단계), 그 자리에 **8개 시장 전부의
발표 배당(`PUB_*`)이 적재**됐다. 결측 0건이다.

| GAME_TYPE | 행 | 면 | 오버라운드(합산) |
|---|---|---|---|
| `야구 승1패` | 2,697 | 3 (승 / 1점차 / 패) | 1.1519 |
| `야구 전반 승무패` | 591 | 3 (**진짜 무승부** 86건) | 1.1521 |
| `야구 승패` | 2,676 | 2 | 1.1412 |
| `야구 핸디캡` | 2,697 | 2, `COND = H ±1.5/2.5` | 1.1411 |
| `야구 언더오버` | 2,697 | 2 (언더/오버), `COND = U/O n.5` | 1.1413 |
| `야구 SUM` | 2,697 | 2 (홀/짝) | 1.1408 |
| `야구 전반 핸디캡` / `전반 언더오버` | 591씩 | 2 | 1.1415 |

### 16.1 `TwoWayOddsEstimator` 폐기 — 가격은 거의 맞았고 **픽이 틀렸다**

§13에서 승패 행에 `PUB_*`가 없어 3면 배당에서 2면 가격을 유추했다. 이제 실측이 있으므로 추정기를
실측과 직접 대조할 수 있다(2,676경기 페어링, 키는 `FixtureKey` 7개 — 전 행 매칭):

| | 값 |
|---|---|
| 홈 가격 평균 절대오차 | 1.51% |
| 원정 가격 평균 절대오차 | 1.71% |
| **우세팀 일치율** | **0.8558** |

가격 오차 1.5~1.7%는 §13에서 실현 `TOTAL_DIV`로 측정한 1.60%와 일치한다 — 추정기는 **숫자로는
합격이었다.** 그런데 **7경기 중 1경기에서 다른 팀을 우세팀으로 지목한다.** 승패 계열은 전부
"우세팀"이나 "가격이 x 이상인 팀"으로 픽을 정하므로, 오차가 작다는 것은 픽이 맞다는 것과 다른
얘기였다. §13~§15의 승패 수치는 전부 14.4%가 잘못 뒤집힌 픽 위에서 측정된 것이다.

추정기는 **폴백으로 남기지 않고 삭제했다.** 근사적으로 맞는 가격은 확신을 갖고 틀린 픽을 만든다 —
백필 `*_DIV`를 지운 것과 같은 이유다(§9 정정).

부수적으로 드러난 것: 추정기의 상수 `PUBLISHED_OVERROUND = 1.13629`(사용자 제공 24경기)는
**상수가 아니었다.** 승패 오버라운드는 260421~260712 약 1.143 → 260713~260817 약 1.136으로
이동했고, 260818 발표분도 1.136이다. 0.7%p 이동은 **쫓고 있는 엣지와 같은 크기**다. 이제 마진은
경기별로 `TwoWayOdds.overround()`가 계산한다.

### 16.2 재탐색 결과 — 게이트 통과, 감사에서 **부결**

실측 가격으로 4계열(`FAVORITE`, `FAVORITE_2WAY`, `WIN_RATE_ODDS`, `WIN_RATE_ODDS_2WAY`)을
260421~260817 전 구간에서 재탐색(후보 404, 검증 14, seed 42, train 0.7).

`WIN_RATE_ODDS_2WAY {combinedN=2, num=20, rankLimit=9, x=1.6, y=1.6}`가 **전 게이트 통과**:

| | 학습 | 검증 |
|---|---|---|
| 다리 | 928 | 372 |
| 레그 적중률 | 0.4688 | 0.5054 |
| 레그 초과수익 | +4.68% | **+10.60%** |
| t | 1.43 | **2.06** |
| 학습·검증 불일치 | | 0.97σ |

세부 구간 감사도 처음에는 좋아 보였다 — 8개 구간 중 7개 양수:

| 구간 | 다리 | 적중률 | 레그 초과수익 |
|---|---|---|---|
| T1 260421~260511 | 208 | 0.457 | +3.93% |
| T2 260512~260601 | 230 | 0.509 | +10.98% |
| T3 260602~260622 | 254 | 0.461 | +3.21% |
| T4 260623~260712 | 236 | 0.449 | +0.76% |
| V1 260713~260721 | 70 | 0.500 | +7.21% |
| V2 260722~260730 | 106 | 0.443 | −2.45% |
| V3 260731~260808 | 90 | 0.533 | +15.28% |
| V4 260809~260817 | 106 | 0.547 | +21.93% |

**부결시킨 것은 이웃 격자 감사다.** `num=20, combinedN=2`를 고정하고 `rankLimit × x × y` = 3×5×4 =
60칸을 전수 백테스트해 학습·검증을 분리하면:

| rankLimit | 학습 양수 칸 | 합산 학습 초과수익 | 합산 **검증** 초과수익 |
|---|---|---|---|
| 6 | 7/17 | −0.38% | +1.00% |
| **9** | **7/19** | **−0.53%** | **+10.11%** |
| 12 | **17/19** | **+1.26%** | +4.45% |

`rankLimit=9`에서 **어떤 파라미터를 골라도 검증 구간이 +10%대로 올라간다.** 합산 검증 +10.11%는
채택 후보의 +10.60%와 사실상 같다 — 즉 그 후보의 검증 성적은 **격자에서 아무 칸이나 뽑은 것과
구별되지 않는다.** t = 2.06은 규칙이 아니라 **구간을 측정한 것**이다. 같은 격자의 합산 학습이
−0.53%라는 점이 결정적이다: 폼 효과가 실재한다면 학습 구간에도 보여야 한다.

"원정팀이 그 구간에 잘했을 뿐"이라는 더 단순한 설명은 **기각됐다**. 승패 행 전체에 무조건 원정 베팅:
학습 −12.66%(초과 −0.15%p), 검증 −10.96%(초과 **+1.04%p**). 구간 효과 +10%p 중 1%p만 설명한다.
남은 9%p는 폼 필터가 발동한 부분표본에 국지적이며, 학습 구간에는 없다.

- **판정: 위양성(5번째).** `pick_mstr` 확정 시뮬레이션은 하지 않았다.
- 원장(`research/experiments.jsonl`)에는 `goalAchieved: true`로 남아 있다. **원장은 파이프라인 판정의
  기록이고, 이 절이 그 판정에 대한 감사다** — 원장을 사후 수정하지 않는 것이 §10 이후의 규칙이다.
- 유일하게 학습 구간에서 일관된 구조는 `rankLimit ≈ 12`(19칸 중 17칸 양수, 합산 +1.26%)이며,
  실용 하한 2%p 미만이다. 다음 후보로 **선언 후** 검증할 가설이지, 지금 소급 교체할 값이 아니다.

### 16.3 남은 표면 — 미탐색 6개 시장

핸디캡·언더오버·SUM·전반 3종은 **아직 어떤 알고리즘도 읽지 않는다**(모든 로딩이 game type으로
필터링하므로 오염은 없다). 이 프로젝트에서 가장 큰 미탐색 영역이다:

- **`야구 언더오버`** — 신호가 팀 승패가 아니라 **득점 총합**이다. 현재 어떤 계열도 `RES1`/`RES2`를
  보지 않으므로 taxonomy 상 완전히 새로운 계열이고, 마진도 가장 낮다(1.1413).
- **`야구 핸디캡`** — 2.5점 기준선이라 **승1패의 "2점차 이상"과 사실상 같은 질문**이다. 승1패 발표
  배당과의 정합성 검증에 쓸 수 있다(측정이지 선택이 아니므로 D2에 걸리지 않는다).
- **`야구 전반 승무패`** — 유일하게 **진짜 무승부**가 있는 시장(86/591 = 14.6%). 3면 구조가 승1패와
  다르므로 `ThreeWayOdds`를 그대로 재사용할 수 있다.

단, §15의 결론은 그대로다: 검증 창 자체가 작아 **어느 계열이든 in-sample로는 t ≥ 2를 신뢰할 수 없다.**
새 시장은 표본을 넓히지만(경기당 여러 시장) 같은 날짜들을 다시 쓰는 것이므로 **날짜 상한을 늘리지
않는다.** 판정은 전진 검증으로 옮겨야 한다.

---

## 17. 전진 검증 1일차 — `WIN_RATE_ODDS_2WAY` (2026-08-17 기록, 260818 경기)

§16.2가 부결된 직후이므로 **후보가 수치로 자격을 얻어서 시작한 것이 아니다.** 전진 검증은 바로
그런 후보를 위한 도구다: 파라미터를 어떻게 골랐든 측정이 오염되지 않는다.

**동결한 파라미터는 파이프라인이 학습 구간만 보고 고른 것**을 그대로 썼다:
`{combinedN=2, num=20, rankLimit=9, x=1.6, y=1.6}`, `paramSignature=combinedN=2,num=20,rankLimit=9,x=1.6,y=1.6`.

§16.2 감사에서 `rankLimit=12`가 학습 일관성이 더 좋아 보였지만 **바꾸지 않았다.** 그 선호는 검증
컬럼을 본 뒤 생긴 것이고, 검증 수치를 보고 파라미터를 고르는 것이 금지된 안티패턴이다
(`.claude/skills/algo-search/SKILL.md`). 동결값의 출처는 "학습 구간 최적"이어야 한다.

기록: 15경기 → 5슬립 / 10레그, `pick_forward` 1~5, 전부 `PENDING`, 정액 1,000원.

| 슬립 | 버킷 | 조합배당 | 레그 |
|---|---|---|---|
| 1 | MLB | 2.74 | 미네소타/애틀랜타 패 1.65 · 피츠버그/디트로이트 패 1.66 |
| 2 | MLB | 4.71 | 탬파베이/볼티모어 패 2.17 · 시카고컵스/화이트삭스 패 2.17 |
| 3 | MLB | 6.18 | 캔자스시티/애슬레틱스 패 2.19 · 필라델피아/마이애미 패 2.82 |
| 4 | KBO_NPB | 2.84 | LG/KT 패 1.66 · NC/두산 패 1.71 |
| 5 | KBO_NPB | 3.92 | 한화/KIA 패 1.75 · 롯데/키움 패 2.24 |

레그별 `backed_odds`가 `match_schedule.PUB_AWAY_DIV`와 전부 일치하고, `overround`는 상수가 아니라
경기별 실측값(0.1352~0.1373)으로 저장됐다 — 배관이 실측 가격을 쓰고 있다는 확인이다.

**즉시 눈에 띄는 것: 10레그 전부 `패`다.** 승패 시장에서 홈 가격이 더 짧으므로 `패`를 내는 두
분기("하위권 홈이 1.6 이하", "상위권 원정이 1.6 이상")가 가격 분포상 더 자주 발동한다. 이 후보가
**"홈팀 페이드"에 가까운 것 아닌가**, 폼 필터가 실제로 기여하는 것이 있는가가 여기서 나오는 질문이다.

> **정정 (§17.2).** 위에서 이것을 "`x = y = 1.6`의 구조적 성질"이라고 썼는데, 하루치 10레그로
> 단정한 것이었다. 36일 346레그로 재측정하면 `패` 229 / `승` 117 — 2:1 편향이지 규칙이 아니다.
> 260818의 10/10은 우연이다. 그리고 폼 필터의 기여는 §17.2에서 **+9.46%p로 측정됐다** — 페이드가
> 아니다.

정산: 경기 후 `POST /api/forward-picks/settle?algorithmCode=WIN_RATE_ODDS_2WAY&userName=NSC`.
누적 다리가 수백 단위에 이르기 전에는 어떤 판정도 하지 않는다 — 5슬립은 표본이 아니다.

### 17.1 리포팅 배관 (2026-08-17)

§17을 기록한 직후 드러난 문제: **전진 검증이 자기 질문에 답할 수 없었다.** `generate`와 `settle`만
있고 읽기 엔드포인트가 없었으며, 초과수익·t값을 계산하는 코드는 `BacktestMetrics` 하나뿐인데 그것은
`pick_forward`를 보지 않았다. 프론트엔드에는 화면이 아예 없었다.

축적이 달력에 묶여 있다는 점이 이것을 1순위로 만든다. 하루 약 10레그이므로 300레그면 30일이고,
사전 기록은 append-only라 **빠뜨린 날은 영구 손실**이다. 30일 뒤에 "읽을 방법이 없다"를 발견하는 것이
최악의 순서다.

- **판정은 탐색과 같은 `research.goal`로 한다.** `ForwardReportService`가 정산된 슬립을 `DayOutcome`
  으로 접어 `BacktestMetrics.from`에 넣고 `ExperimentGoal.evaluate`를 호출한다. 전진 검증 전용 지표나
  전용 관문을 만들지 않은 이유는 두 가지다: 척도가 다르면 "백테스트 +7%p vs 전진 +4%p" 비교가
  무의미해지고, 관문이 다르면 in-sample로 통과 못 한 목표를 우회로 통과시킬 수 있다.
- **레그 수치는 기록에서 계산한다.** `ForwardPick.legTally()`가 각 레그에 저장된 `backedOdds`·
  `overround`를 쓴다. 오늘의 데이터로 가격을 다시 뽑으면 측정이 재구성으로 바뀌고, §16.1에서 확인한
  대로 2면 마진은 이동하므로(1.143 → 1.136) 고정 마진 가정은 쫓는 엣지만큼의 오차를 만든다.
  미정산 슬립은 `LegTally.EMPTY` — 결과가 안 난 베팅은 이긴 것도 진 것도 아니다.
- **대조군을 지표에 넣었다.** §17의 "10레그 전부 `패`"를 방치하면 폼 필터의 기여가 영원히 미확인으로
  남는다. `awayBaseline`이 같은 레그에 원정만 걸었을 때의 초과수익을 계산하고,
  `excessOverAwayBaseline`이 차이를 낸다. 차이가 0이면 그 후보는 원정 베팅과 같은 것을 하고 있다는
  뜻이며, 화면이 그렇게 명시한다. `backedSideCounts`는 한쪽 쏠림을 숫자로 드러낸다.
- **커버리지가 운영 지표다.** `coverage.missedYmds` = 경기가 치러졌는데 픽이 없는 날. 복구 불가이므로
  경고로 띄운다. 시스템의 다른 어떤 것도 이 구멍을 보고하지 않는다.

엔드포인트: `GET /api/forward-picks`(목록), `GET /api/research/forward-report`(누적 판정).
화면: `/forward-test`. 검증: 백엔드 127 테스트 통과(신규 13개 — 대조군 산식, 커버리지 결손 탐지,
미정산 제외, 목표 판정 재사용), 프론트 빌드·린트 통과, 기록된 5슬립으로 실제 응답 확인
(`benchmarkOutputMoney` 773 = (1/1.137169)×(1/1.137282)×1000, 방향 쏠림 경고 표시됨).

### 17.2 과거 구간 리플레이 = **배관 검증** (전진 검증이 아니다) (2026-08-17)

"내일 경기를 기다리지 말고 지금까지의 결과로 루프를 돌리면 되지 않나"는 질문에서 나왔다. 답은
**루프를 돌리는 것으로는 전진 검증이 되지 않는다**는 것이다 — 260421~260817은 전부 파라미터를 고르는
데 쓰인 날들이므로, `pick_forward`에 넣어도 §16.2의 구간 효과가 그대로 들어온다. 증거력은 경기가
아직 없었다는 사실에서 나오고, 어느 테이블에 썼는지는 상관없다.

**그러나 리플레이 자체는 반드시 필요했다.** 전진 배관은 실측 데이터로 검증된 적이 없었다 — 단위
테스트는 전부 합성 데이터이고, `FixtureKey` 정산 매칭·베팅시점 가격 기록·레그 집계는 실측 대조가
0이었다. 수 주를 쌓은 뒤 버그를 발견하면 그 기간이 전부 날아간다.

절차: `match_schedule`에 260713~260817의 경기 전 컬럼만 복사(1,494행) → `userName=REPLAY`로 36일
generate → settle → 같은 파라미터·같은 구간의 백테스트와 대조. `REPLAY`로 격리해 실제 기록(NSC)의
마틴게일 리플레이와 커버리지를 건드리지 않았고, **검증 후 산출물은 삭제했다** — 학습 구간에서 나온
173슬립을 `pick_forward`에 남겨두면 전진 검증 증거로 오인될 수 있고, 그것이 백필 DIV 컬럼과 정확히
같은 함정이기 때문이다.

#### 결과 1 — 배관은 정상, 그리고 **다리 26개가 중복이었다**

| | 백테스트(검증 구간) | 전진 배관 리플레이 |
|---|---|---|
| 슬립 | 186 | 173 |
| 다리 | 372 | **346** |
| 레그 적중률 | 0.5054 | 0.5029 |
| 레그 초과수익 | +10.60% | +10.16% |
| **t** | **2.06** | **1.90** |

346레그 전부가 `FixtureKey`로 결과 행에 매칭됐다(미해결 0건). 배관은 정상이다.

차이의 원인은 **같은 경기가 두 회차에 등록되는 것**이다. 전진 경로는 `PickUniverse.distinctFixtures`
로 접지만, **비스테이킹 백테스트는 `(year, round, ymd)` 평면 그룹핑을 쓰므로 접지 않는다**(CLAUDE.md에
명시된 의도적 선택 — 원장 수치의 과거 비교 가능성을 지키기 위한 것). 전 구간에서 2,697행 중 222행
(8.2%), 검증 구간에서 749행 중 58행(7.7%)이 중복이다.

**그 결과 t가 2.06 → 1.90으로 내려가고, 같은 후보가 t ≥ 2.0 관문을 통과하지 못한다.** 중복 경기는
독립 레그가 아니므로 표준오차를 과소 추정하게 만든다. 제거된 26레그가 평균 +16.5%p의 초과수익을
싣고 있었다는 점(39.43 → 35.15)은 방향도 나쁘다 — 잘 맞은 경기가 두 번 세어졌다.

즉 §16.2의 "달성" 판정은 **구간 효과와 중복 계수라는 서로 독립인 두 가지** 위에 서 있었다. 부결
결론은 그대로이고, 근거가 하나 더 늘었다.

#### 결과 2 — 폼 필터는 "홈팀 페이드"가 아니다

§17이 남긴 질문의 답이다. 같은 346레그에 무조건 원정만 걸었을 때와 비교하면:

| | 다리 | 적중률 | 레그 초과수익 | t |
|---|---|---|---|---|
| 픽 | 346 | 0.5029 | **+10.16%** | 1.90 |
| 무조건 원정(대조군) | 346 | 0.4711 | +0.70% | 0.14 |
| 차이 | | +3.2%p | **+9.46%p** | |

대조군이 +0.70%(t = 0.14)로 사실상 0인 것은 de-vig 항등식이 예측하는 그대로다. 초과수익 10.16%p 중
**9.46%p가 선택에서 나온다.** 픽 방향도 `패` 229 / `승` 117이라 한쪽 베팅이 아니다. §17에서 260818
하루치 10/10을 보고 "구조적"이라고 쓴 것은 과했고 위에 정정했다.

이것은 후보를 되살리는 근거가 **아니다** — 여전히 학습 구간이 포함된 in-sample 수치다. 다만 "이
계열이 사실 원정 베팅일 뿐"이라는 가장 값싼 반증은 기각됐고, 전진 검증을 계속할 이유가 된다.

#### 결과 3 — 커버리지 경보의 한계 (미검증)

리플레이는 36일 전부 베팅해서(`bettingDays 36 / dayCount 36`) `missedYmds`가 비었다. 따라서
**"알고리즘이 그날 픽을 안 냈다"와 "루프가 안 돌았다"를 구분하지 못하는 문제는 검증되지 않은 채로
남아 있다.** 필터가 하루 전체를 걸러내면 커버리지는 그것을 결손으로 보고한다. 기록만으로는 구분이
불가능하므로(사전 픽은 append-only이고 0슬립 마커를 남기지 않는다), 경보는 "손실"이 아니라
**"확인 필요"**로 읽어야 한다.
